services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      APP_DB_PATH: /app/data/app.db
    volumes:
      - app_data:/app/data

volumes:
  app_data:
